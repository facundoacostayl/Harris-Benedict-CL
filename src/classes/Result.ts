export class Result {

    constructor(
        private sexo: string,
        private peso: number,
        private tipo: string,
        private estatura: number,
        private medida: string,
        private edad: number,
        private actividad: string
    ){}

    calculate() : [] {
        let result : [] = [];

        let tmb: number;

        return result;
    }
}